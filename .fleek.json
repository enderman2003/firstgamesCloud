{
 "build": {
   "image": "ubuntu:latest",
   "command": "chmod +x ./nakama && ./nakama --config ./data/config.yml",
   "publicDir": "build",
   "baseDir": "./",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}
