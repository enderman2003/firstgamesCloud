apiVersion: apps/v1
kind: Deployment
metadata:
  name: enderman2003
spec:
  selector:
    matchLabels:
      app: nakama
  replicas: 1
  template:
    metadata:
      labels:
        app: firstgamescloud
    spec:
      containers:
      - name: nakama
        image: okteto.dev/nakama:latest
        ports:
        - containerPort: 8080
